# ============================================
# E7 Shop Bot — 依赖清单
# 合并自 ALAS 框架 + E7 原有依赖
# ============================================

# ── 核心框架 ──
numpy>=1.24
opencv-python>=4.8
rich>=13.0
Pillow>=10.0
PyYAML>=6.0

# ── OCR ──
rapidocr-onnxruntime>=1.3
cnocr>=2.2
paddleocr>=3.4.0

# ── 设备控制 ──
uiautomator2>=3.5.0
zerorpc>=0.6.3

# ── WebUI ──
pywebio>=1.8.0
fastapi>=0.110
uvicorn[standard]>=0.29
starlette>=0.37

# ── 工具 ──
requests>=2.31
inflection>=0.5
scipy>=1.12
tqdm>=4.66
imageio>=2.33

# ── 通知 ──
onepush>=2.0

# ── 可选：Discord RPC ──
pypresence>=4.3

# ── 可选：文件变更监听 ──
watchdog>=4.0

# ── 可选：Git 更新 ──
GitPython>=3.1

# ── 开发/测试 ──
pytest>=9.0.2
hypothesis>=6.151.10
