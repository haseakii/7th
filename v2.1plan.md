# v2.1 — ALAS 风格调度器：整体设计

## 目标

迁移 ALAS 调度器机制，实现动态任务发现与调度循环，替代之前硬编码的 `run_secret_shop()` 直通调用。

**核心价值：** 多任务可配置、WebUI 动态生效、调度间隔可调。

---

## 架构总览

```
main.py / gui.py (启动入口)
    │
    └─ E7AutoScript (alas.py)
         │
         ├─ init() ─── 加载配置 + 连接设备
         │
         └─ loop() ─── 调度主循环
              │
              ├─ config.get_next()
              │    ├─ config.load()           ← 每次重读 JSON（WebUI 变更即时生效）
              │    ├─ get_next_task()
              │    │    ├─ 遍历 self.data (JSON 所有顶层 key)
              │    │    ├─ Function(data) 构造任务对象
              │    │    └─ Filter 按优先级排序
              │    └─ 返回最早到期的任务
              │
              ├─ run(command)
              │    └─ self.__getattribute__(command)()  ← inflection.underscore 派发
              │
              ├─ config.task_delay()
              │    └─ 更新 JSON 中 Scheduler.NextRun
              │
              └─ 循环（每轮重读 config）
```

---

## 组件设计

### 1. Config 系统 (`module/config/config.py`)

**核心类：`E7Config`**

MRO 继承链：
```
E7Config → ConfigUpdater → ManualConfig → GeneratedConfig → object
```

**数据流：**
```
args.json (参数定义)
    │
    ├─ deep_iter(args, depth=3) → {path: {type, value, display, ...}}
    │
    └─ _get_defaults() → 提取 value 作为默认值
         │
         └─ 与 config/default.json 合并 → self.data
              │
              ├─ __getattr__ → ARG_MAP 映射 → self.get(path)
              └─ get_next() → Function(data) 读取 Scheduler 组
```

**调度器接口：**

| 方法 | 功能 | 备注 |
|---|---|---|
| `load()` | 从磁盘重读 JSON | 每轮循环调用，WebUI 变更即时生效 |
| `get_next()` | 返回下一个待运行任务 | 抛出 RequestHumanTakeover 表示无任务 |
| `get_next_task()` | 遍历所有任务，分 pending / waiting | Filter 按优先级排序 |
| `task_delay()` | 设置 Scheduler.NextRun | success/failure/server_update/target/minute |
| `task_call()` | 立即调用另一个任务 | force_call 可忽略禁用状态 |
| `task_stop()` | 停止当前任务（抛 TaskEnd） | 静态方法 |
| `task_switched()` | 检查是否切换到其他任务 | load() + get_next() 对比 |
| `check_task_switch()` | 如果任务已切换则停止 | 任务内定期调用 |
| `is_task_enabled()` | 检查指定任务是否启用 | |

**辅助类：`Function`**

代表调度系统中的一个可运行任务。从 JSON dict 构造，读取 `Scheduler` 子组的数据：

| 属性 | 来源 |
|---|---|
| `enable` | `Scheduler.Enable` |
| `command` | `Scheduler.Command` |
| `next_run` | `Scheduler.NextRun` |

### 2. 调度循环 (`alas.py`)

**类：`E7AutoScript`**

```
loop() 每轮迭代:
  1. task = config.get_next()
  2. 若 task.next_run > now: wait(≤60s 轮询 stop_event)
  3. command = inflection.underscore(task.command)
  4. config.task = task
  5. success = run(command)
  6. config.task_delay(success=success, task=task.command)
  7. 失败计数 ≥ 3 → 退出
  8. config.load()  ← 重读 JSON
```

**异常链：**

| 异常 | 含义 | run() 返回 |
|---|---|---|
| `TaskEnd` | 正常结束 | True |
| `GameStuckError` | 游戏卡死，重启设备 | False |
| `ScriptError` | 脚本错误，需要人工介入 | 抛出（终止循环） |
| 其他异常 | 未知错误 | False |

**任务注册：**
- 在 `config/*.json` 中增加带 `Scheduler` 子组的顶层 key
- 在 `alas.py` 中增加同名方法（蛇形命名）
- 重启或 WebUI 修改 Enable 即可自动生效

### 3. 参数定义 (`args.json`)

**结构约定（ALAS 标准）：**
```json
{
  "GroupName": {
    "Main": {              ← 子组级别
      "Enable": {
        "type": "checkbox",
        "value": false
      }
    }
  }
}
```

**当前定义的任务组：**

| 组 | 子组 | 用途 |
|---|---|---|
| `Scheduler` | `Main` | 调度器共享参数（Enable, NextRun, Command, SuccessInterval, FailureInterval, ServerUpdate） |
| `SecretShop` | `ShopItems`, `RefreshSettings`, `Thresholds`, `StopConditions` | 秘密商店配置 |
| `Device` | `Connection` | 设备连接配置 |
| `Webui` | `Server` | WebUI 服务配置 |

### 4. Config 数据 (`config/*.json`)

**结构：** 每个任务顶层 key 下包含自身配置和 `Scheduler` 子组。
```json
{
  "SecretShop": {
    "Scheduler": {
      "Enable": true,
      "NextRun": "2020-01-01 00:00:00",
      "Command": "SecretShop",
      "SuccessInterval": 0,
      "FailureInterval": 120,
      "ServerUpdate": "00:00"
    },
    "ShopItems": { ... },
    "RefreshSettings": { ... }
  }
}
```

`Scheduler` 组嵌入在 config 数据的每个任务下，而不是一个独立顶层 key。`args.json` 中顶层的 `Scheduler` 只定义参数的结构和默认值，不参与 `_get_defaults()` 的 config 默认生成。

### 5. WebUI 集成 (`module/webui/`)

**进程模型：**
```
Web UI (uvicorn / main thread)
    │
    └─ ProcessManager (subprocess)
         └─ run_process()
              ├─ func == "alas" / "SecretShop" → E7AutoScript.loop()
              └─ func == other → E7AutoScript.run(command)
```

**配置监听：**
```
_init_alas_config_watcher()
    └─ get_alas_config_listen_path(ALAS_ARGS)
         └─ deep_iter(args, depth=3) → {path: arg_dict}
              └─ pin_on_change(name, callback) → 修改时触发 put_queue
```

**深度约定：**
- `deep_iter(args)` 使用 `depth=3`：`[Group, SubGroup, Arg]` → arg 定义 dict
- `deep_iter(config)` 使用 `depth=3`：`[Task, SubGroup, Arg]` → 配置值
- `set_group()` 先 `depth=1` 遍历任务子组，再 `depth=1` 遍历子组内参数

---

## 异常与边界处理

| 场景 | 处理方式 |
|---|---|
| 无任务启用 | `get_next()` 抛出 `RequestHumanTakeover`，`loop()` 退出 |
| 任务连续失败 3 次 | `loop()` 记录失败计数，达到阈值退出 |
| 游戏卡死 | `GameStuckError` → `_restart_device()` → 失败计数 +1 |
| 任务调度在未来 | `wait()` 分块轮询（每 60s 检查 stop_event） |
| WebUI 关闭 | `ProcessManager.stop()` → `process.kill()`（立即终止） |
| 设备连接失败 | `init()` 返回 False，循环不启动 |
| YAML 迁移备份已存在 | `os.replace()` 强制覆盖（Windows 兼容） |
| 配置 NextRun 为空 | `ensure_time` → `DEFAULT_TIME`（2000-01-01） |
| JSON 中 NextRun 为字符串 | `parse_time()` 统一转 datetime |
| task_delay 写入 JSON | `.strftime()` 转字符串再写 |

## v2.1 修复的 Bug

| # | 严重度 | 文件 | 问题 | 修复 |
|---|---|---|---|---|
| BUG-1 | CRITICAL | `config_updater.py:129` | `write_file()` 用 `filepath_args` 写到 `module/config/argument/` | 改为 `filepath_config` |
| BUG-2 | CRITICAL | `app.py:525-527` | `_save_config` 中 `str - datetime` 导致 TypeError | 明确匹配 `NextRun`，用 `fromisoformat` 解析 |
| BUG-3 | MODERATE | `config.py:412` | YAML 迁移 `Path.rename()` 在 Windows 第二次运行抛 `FileExistsError` | 改用 `os.replace()` |
| BUG-4 | MODERATE | `alas.py:101-105` | `stop_event.wait(large)` 长时间阻塞循环 | 分块轮询每 60s |
| BUG-5 | CRITICAL | `config.py:277` | `task_delay` 写 `datetime` 对象到 `modified`，JSON 序列化失败 | 改用 `.strftime()` 转字符串 |
| BUG-6 | MODERATE | `config.py:453-455` | `Function.__init__` 不解析 `NextRun` 字符串 → 全部进入 error 分支 | 增加 `parse_time()` 统一转换 |

---

## 测试策略

**现有覆盖（99 tests）：**
- `shop_bot_unit.py` — ShopBot 主循环、刷新、恢复
- `purchase_unit.py` — 购买判定、确认弹窗
- `recognizer_unit.py` — 物品识别、分类
- `navigator_unit.py` — 场景检测、导航
- `scene_unit.py` — SceneManager 场景检测

**缺失覆盖（待补充）：**
- `alas.py` 调度循环（init, loop, run, 异常链）
- `config.py` 调度器接口（get_next, task_delay, Function）
- `deep.py` 工具函数（deep_get, deep_set, deep_iter）
- `config_updater.py` 配置持久化

---

## 已知限制与后续

| # | 问题 | 优先级 |
|---|---|---|
| 1 | WebUI 不渲染 `Scheduler` 设置组（当前只通过 JSON 编辑） | LOW |
| 2 | `running` 属性只检查 `_device is not None`，不反映循环状态 | LOW |
| 3 | `stop_event` 是类级属性，多实例共享 | LOW |
| 4 | 没有 `Command` → 自动生成 `NextRun` 的 WebUI 控件 | LOW |
| 5 | `save_callback` 是空方法 | LOW |
| 6 | `valuetype` / `validate` 字段在 args.json 中不存在 | LOW |
| 7 | `shop_bot.py` 重复调用 `device.connect()` | LOW |
| 8 | `save()` 直接写文件，json.dump 中途失败会破坏 JSON | LOW |
| 9 | `_save_config` 使用 `deep_iter(config, depth=3)` 遍历全部配置 | LOW |
| 10 | `ConfigWatcher` 后台线程每 2s 轮询，与 loop() 的 load() 重复 | LOW |

---

## 关键文件一览

| 文件 | 职责 |
|---|---|
| `alas.py` | 主调度器 E7AutoScript：init、loop、run、任务方法 |
| `module/config/config.py` | E7Config：调度接口 + Function 类 + ARG_MAP |
| `module/config/config_generated.py` | 自动生成的类属性（Scheduler_* 等） |
| `module/config/config_manual.py` | 手动配置（SCHEDULER_PRIORITY, SERVER） |
| `module/config/config_updater.py` | 配置读写、合并、默认值生成 |
| `module/config/argument/args.json` | 参数定义（类型、默认值、显示属性） |
| `module/base/filter.py` | ALAS Filter 类（优先级排序） |
| `module/webui/app.py` | WebUI 页面渲染、配置监听、保存 |
| `module/webui/process_manager.py` | 调度器子进程管理 |
| `module/config/utils.py` | 工具函数（parse_time, ensure_time, DEFAULT_TIME） |
| `module/exception.py` | RequestHumanTakeover 等异常 |
| `config/default.json` | 默认配置（Enable = true） |
| `config/template.json` | 模板配置（Enable = false） |
